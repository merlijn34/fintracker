{
  "dependencies": {
    "@nestjs/mapped-types": "*"
  },
  "name": "lendlee",
  "version": "0.0.1",
  "private": true,
  "description": "Loan tracking SaaS application monorepo",
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "test": "turbo run test",
    "lint": "turbo run lint",
    "typecheck": "turbo run typecheck",
    "api:build": "pnpm --filter api build",
    "api:test": "pnpm --filter api test",
    "web:build": "pnpm --filter web build",
    "web:test": "pnpm --filter web test"
  },
  "devDependencies": {
    "turbo": "^2.3.3"
  },
  "packageManager": "pnpm@9.15.4",
  "engines": {
    "node": ">=18.0.0"
  }
}
